<UserControl x:Class="_02_Party.View.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:_02_Party.View"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <TabControl x:Name="tcMain">
            <TabItem x:Name="tiEvento" Header="Evento">
                <local:EventoView></local:EventoView>
            </TabItem>
            <TabItem x:Name="tiCompleanno" Header="Compleanno">
                <local:CompleannoView></local:CompleannoView>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
